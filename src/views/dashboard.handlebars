<div id="dashboard">
    <div class="userData">
      <div id="userNameContainer">
      <p><strong>Welcome: </strong>{{user.first_name}}</p>
      <img id="userAvatar" src="{{user.avatar}}" alt="user avatar">
    </div>
      <p style="color:red; text-transform: uppercase"><strong
          style="color:black; text-transform: capitalize"
        >Role: </strong>{{user.role}}</p>
      <a class="userCartBtn" href="/products"><p>Products</p></a>
      <a class="userCartBtn" href="/api/sessions/logout"><strong>LOGOUT</strong></a>
    </div>
    <div id="dashboardBody">
        <div id="userData">
            <img src="{{user.avatar}}" alt="avatar">
            <div class="userInfoTag">
                <p>Name:</p>
                <strong>{{user.first_name}} {{user.last_name}}</strong>
            </div>
            <div class="userInfoTag">
                <p>Age:</p>
                <strong>{{user.age}}</strong>
            </div>
            <div class="userInfoTag">
                <p>Role:</p>
                <h3><strong>{{user.role}}</strong></h3>
            </div>
        </div>
        <div id="buttonPanel">
            <h2>ADMIN DASHBOARD:</h2>
            <div class="divDashBtn">
                <p>Delete inactive users:</p>
                <a href="#" class="dashBtn">💣 <strong>DELETE USERS</strong> 💥</a>
            </div>
            <div class="divDashBtn">
                <p>Toggle role to premium / user:</p>
                <a href="#" class="dashBtn">🥇 <strong>TOGGLE ROLES</strong> 👤</a>
            </div>
            <div class="divDashBtn">
                <p>Get all users:</p>
                <a href="#" class="dashBtn">👨‍💻 <strong> GENERATE JSON</strong> 📋</a>
            </div>
        </div>
    </div>
</div>